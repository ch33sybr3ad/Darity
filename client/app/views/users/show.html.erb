<section class="container">

  <h1><%= @user.username.capitalize %>'s Page</h1>
  <%= image_tag @user.image_url %>

<% if @user == current_user %>
  <%= link_to "Dare Yourself!", new_user_dare_path(current_user) + "?daree=#{@user.id}" %>
<% else %>
  <%= link_to "Dare #{@user.username.capitalize}!", new_user_dare_path(current_user) + "?daree=#{@user.id}" %>
<% end %>


  <a href="" id="challenged">challenged</a>
  <a href="" id="proposed">proposed</a>
  <a href="" id="pledged">pledged</a>

  <section id="view-dares"></section>


</section>
